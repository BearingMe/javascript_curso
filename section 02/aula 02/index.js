// string
const name = 'Luiz';

// number
const age = 27;

// boolean
const hasHobbies = true;

// undefined
const job = undefined;

// null
const car = null;

// object
const address = {
  city: 'São Paulo',
  country: 'Brasil'
};

// array
const hobbies = ['Sports', 'Cooking'];

// function
const calculateAge = (birthYear) => { return 2020 - birthYear; };

// template string
const hello = `Hello, my name is ${name} and I am ${age} years old.`;

// object destructuring
const { city, country } = address;

// array destructuring
const [hobby1, hobby2] = hobbies;

